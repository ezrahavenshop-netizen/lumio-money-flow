Update the Dashboard page to add the following sections 
below the Available Balance card. Do not remove or change 
Row 1 (bank card) or Row 2 (available balance card).

===================================================
ROW 3 — QUICK ACTIONS
===================================================

Full width section below the balance card.
Label above (uppercase, 11px, muted): "QUICK ACTIONS"

4 equal-width cards in a horizontal row:
  [Send Money]      [ArrowRightLeft icon]  → navigates to /transfer
  [Transaction History] [Clock icon]       → navigates to /history
  [My Cards]        [CreditCard icon]      → navigates to /cards
  [My Profile]      [UserCircle icon]      → navigates to /profile

Each card:
  White background, 12px radius, subtle shadow
  Icon centered in a 44px circle (soft gold background, 
  gold icon)
  Label below icon (14px, navy, IBM Plex Sans 500)
  Hover: translateY(-3px) + gold border + deeper shadow
  Press: scale(0.97)
  Cursor: pointer
  Transition: 200ms ease

Mobile: 2×2 grid

===================================================
ROW 4 — RECENT TRANSACTIONS PREVIEW
===================================================

Full width section below quick actions.

Header row:
  Left: "Recent Transactions" (serif, 20px, navy)
  Right: "View All →" link (gold, 13px) → navigates to /history

Show the 5 most recent transactions from the 
transactions array in AppContext (sorted by date, 
newest first).

Each transaction row:
  Left: category icon in a 40px circle
    income → green bg, TrendingUp icon
    transfer → navy bg, ArrowRightLeft icon
    housing → amber bg, Home icon
  
  Center:
    Reference (14px, navy, semibold)
    Date formatted as "15 Mar 2019" (12px, muted)
  
  Right:
    Amount with ▲ prefix + green color for credits
    Amount with ▼ prefix + red color for debits
    £ prefix, 15px, bold
    Status badge below amount:
      "Successful" (green pill, 10px)
      "Pending" (amber pill, 10px)

Row separator: 1px #E5E7EB border between each row
Row hover: soft gold background tint, cursor pointer
Row click: navigates to /history

Card container:
  White background, 12px radius, elevated shadow
  Padding: 24px

Empty state (if no transactions):
  Centered: clock icon + "No transactions yet"

Mobile: same layout, full width

===================================================
ROW 5 — ACCOUNT SUMMARY STRIP
===================================================

Full width, below recent transactions.
Light gray background (#F7F8FA), 12px radius, 24px padding.

3 items displayed horizontally, divided by vertical 
separators:

  Item 1:
    Label (uppercase, 11px, muted): "ACCOUNT NUMBER"
    Value (14px, navy, monospace): **** **** **** 4821
    Small "Copy" button (Lucide Copy icon, gold, 12px)
    On click: copies full account number to clipboard
    Toast: "Account number copied"

  Item 2:
    Label (uppercase, 11px, muted): "ACCOUNT TYPE"
    Value (14px, navy): Lumio Premier
    Gold pill badge: "● Active"

  Item 3:
    Label (uppercase, 11px, muted): "MEMBER SINCE"
    Value (14px, navy): March 2019
    Green badge: "✓ KYC Verified"

Mobile: stacks vertically, no dividers

===================================================
ANIMATION
===================================================

All 3 new rows mount with staggered fade-in:
  Row 3 quick actions: 100ms delay, stagger 60ms per card
  Row 4 transactions: 200ms delay, stagger 50ms per row
  Row 5 strip: 350ms delay

===================================================
LAYOUT NOTE
===================================================

Final dashboard order from top to bottom:
  1. Greeting banner (dismissible)
  2. Bank card (Row 1)
  3. Available balance card (Row 2)
  4. Quick actions (Row 3) ← NEW
  5. Recent transactions (Row 4) ← NEW
  6. Account summary strip (Row 5) ← NEW

All sections full width.
Consistent 24px gap between each row.
Page has comfortable bottom padding: 64px.